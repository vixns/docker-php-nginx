#!/bin/sh
exec 2>&1
[ -n "${HAS_HAPROXY}" ] || touch /etc/service/haproxy/down
[ -n "${HAS_PROXYSQL}" ] || touch /etc/service/proxysql/down

PATH=/usr/local/bin:/usr/local/sbin:/bin:/sbin:/usr/bin:/usr/sbin

exec env - PATH=$PATH \
runsvdir -P /etc/service 'log: ...........................................................................................................................................................................................................................................................................................................................................................................................................'
